export enum Gender {
    Male,
    Female,
}

export enum Position {
    Frontend,
    Backend,
    Analyst,
    Manager,
    Designer,
}

export enum Technology {
    CSharp,
    React,
    Java,
    PHP,
    Figma,
    Word,
}
